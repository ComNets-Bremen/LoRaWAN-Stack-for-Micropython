[project]
name = "lorawan-stack-for-micropython"
version = "0.1.0"
description = "This is a LoRa and LoRaWAN stack for Micropython"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "esptool>=5.2.0",
    "mpremote>=1.27.0",
]
